#include <stdio.h>
/**
 * main - prints name
 * @argc: number of args
 * @argv: array that contain the program comd
 * Return: 0 - success
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
