#include "main.h"
/**
 * _memcpy - memory copy
 * @dest: destination
 * @src: source
 * @n: size
 * Return: *dest
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int g;

	for (g = 0; g < n; g++)
	{
		dest[i] = src[i];
	}
	return (dest);
}
