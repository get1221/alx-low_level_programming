#include <stdio.h>
/**
 * main - main block
 *
 * Descriptions: Print numbers from zero to nine
 * Return: Always 0
 */
int main(void)
{
int num;

for (num = 0; num < 10; num++)
printf("%i", num);
putchar('\n');
return (0);
}
